dataset: db_bio
data_path: data/db_bio
attacker_extensions: data/db_bio/db_bio_bk_sum_realistic.jsonl
model_name: distilbert-base-uncased
output_root: ../../../netscratch/yay/tri_pipelines/db_bio/_checkpoints
max_records: null
max_length: 512

training:
  finetuning_epochs: 15
  batch_size: 16
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  p_agg: avg