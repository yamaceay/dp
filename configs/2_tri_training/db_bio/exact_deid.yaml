dataset: db_bio
data_path: data/db_bio
split: indices/db_bio/test.txt
attacker_extensions: data/db_bio/tri_preprocessing/exact_deid.jsonl
model_name: distilbert-base-uncased
output_root: ../../../netscratch/yay/tri_pipelines/db_bio/_checkpoints/exact_deid
max_records: null
max_length: 512

training:
  finetuning_epochs: 20
  batch_size: 16
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  early_stop_threshold: 0.95