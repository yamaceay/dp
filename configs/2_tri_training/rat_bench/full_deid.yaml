dataset: rat_bench
data_path: data/rat_bench
attacker_extensions: data/rat_bench/tri_preprocessing/full_deid.jsonl
model_name: distilbert-base-uncased
output_root: ../../../netscratch/yay/tri_pipelines/rat_bench/_checkpoints/full_deid
max_records: null
max_length: 512

training:
  finetuning_epochs: 20
  batch_size: 16
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  early_stop_threshold: 0.95
