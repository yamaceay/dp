dataset: tab
data_path: data/TAB/tab.json
attacker_extensions: data/TAB/test_bk_sum_realistic.jsonl
model_name: roberta-large
output_root: ../../../netscratch/yay/tri_pipelines/tab/_checkpoints_realistic
max_records: null
max_length: 512

training:
  finetuning_epochs: 25
  batch_size: 8
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  early_stop_threshold: null
  learning_rate: 8e-5
  scheduler_type: cosine
  optimizer_type: adamw
  warmup_ratio: 0.03
  weight_decay: 0.005
