dataset: tab
data_path: data/tab
attacker_extensions: data/tab/tri_preprocessing/full.jsonl
model_name: distilbert-base-uncased
output_root: ../../../netscratch/yay/tri_pipelines/tab/_checkpoints/full
max_records: null
max_length: 512

training:
  finetuning_epochs: 20
  batch_size: 16
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  p_agg: avg
  early_stop_threshold: 0.95

  # finetuning_epochs: 30
  # batch_size: 8
  # use_pretraining: true
  # pretraining_epochs: 1
  # per_step: null
  # loss_type: focal
  # focal_gamma: 5.0
  # focal_ignore_pt: 0.95
  # exclude_stopwords: true
  # p_agg: avg
  # early_stop_threshold: 0.95
