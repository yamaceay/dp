dataset: tab
data_path: data/tab
attacker_extensions: data/tab/tri_preprocessing/full_deid.jsonl
model_name: distilbert-base-uncased
output_root: ../../../netscratch/yay/tri_pipelines/tab/_checkpoints/full_deid
max_records: null
max_length: 512

training:
  finetuning_epochs: 20
  batch_size: 16
  use_pretraining: true
  pretraining_epochs: 1
  per_step: null
  early_stop_threshold: 0.95
