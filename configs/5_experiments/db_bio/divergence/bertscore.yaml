dataset: db_bio
data_in: data/db_bio
split: indices/db_bio/test/50_eval/{task_id}.txt
annotations_in:
  - outputs/db_bio
metric:
  type: bertscore
  model_type: roberta-large
  batch_size: 8
output:
output:
  format: jsonl
  file: logs/divergence/db_bio/bertscore_task_{task_id}.jsonl
