compensate_epsilon: false
add_probability: 0.0
delete_probability: 0.0

pii_annotator: models/pii_detectors/tab/20251019_140402
pii_threshold: 0.7

explainability: greedy
explainer_path: models/tri_pipelines/tab/20251018_201114

pii_chunking:
  enabled: true
  max_length: 512
  overlap: 50

tri_chunking:
  enabled: true
  max_length: 512

dpmlm_chunking:
  enabled: true
  max_length: 512

# Model parameters (can be overridden in code)
# model_checkpoint: "roberta-base"
# clip_min: -3.2093127
# clip_max: 16.304797887802124
# k_candidates: 5
# use_temperature: true