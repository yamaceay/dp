params:
  ks: ["*"]

T: 5

precomputation:
  risk_scores: data/db_bio/tri_risk/shap_subset.jsonl
  result_in: ../../../netscratch/yay/_ext_outputs_subset/db_bio/presidio/{run_timestamp}_db_bio_presidio.jsonl

explainer:
  name: shap
  tri_pipeline: ../../../netscratch/yay/tri_pipelines/db_bio/_checkpoints/full/full

token_selection:
  name: until_k

verbose: true
